"""Phase 4 test suite for the IT / Ticket Auto-Resolution Agent.

Uses the standard-library ``unittest`` framework (no external dependency) so the
tests run in any environment, online or offline.

Run from the project root:

    python -m unittest discover -s tests -v
"""
